import FileComponent from "@/components/file";

export default function Home() {
    return (
        <div className="flex items-center justify-center w-full h-screen p-10 overflow-auto">
            <FileComponent />
        </div>
    );    
}
